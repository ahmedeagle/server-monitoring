name: Deploy to AWS ECS

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  ECR_API_REPOSITORY: servermonitoring-api
  ECS_CLUSTER: servermonitoring-cluster
  ECS_API_SERVICE: servermonitoring-api-service
  API_CONTAINER_NAME: api

jobs:
  deploy:
    name: Deploy to ECS
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build, tag, and push API image to Amazon ECR
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        IMAGE_TAG: ${{ github.sha }}
      run: |
        docker build -t $ECR_REGISTRY/$ECR_API_REPOSITORY:$IMAGE_TAG -f src/Presentation/ServerMonitoring.API/Dockerfile .
        docker tag $ECR_REGISTRY/$ECR_API_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_API_REPOSITORY:latest
        docker push $ECR_REGISTRY/$ECR_API_REPOSITORY:$IMAGE_TAG
        docker push $ECR_REGISTRY/$ECR_API_REPOSITORY:latest
        echo "API_IMAGE=$ECR_REGISTRY/$ECR_API_REPOSITORY:$IMAGE_TAG" >> $GITHUB_ENV

    - name: Create IAM roles for ECS
      run: |
        # Create ECS Task Execution Role
        EXEC_ROLE_EXISTS=$(aws iam get-role --role-name ecsTaskExecutionRole-ServerMonitoring --query 'Role.RoleName' --output text 2>/dev/null || echo "None")
        
        if [ "$EXEC_ROLE_EXISTS" = "None" ]; then
          echo "Creating ecsTaskExecutionRole-ServerMonitoring..."
          
          cat > trust-policy.json <<EOF
        {
          "Version": "2012-10-17",
          "Statement": [
            {
              "Effect": "Allow",
              "Principal": {
                "Service": "ecs-tasks.amazonaws.com"
              },
              "Action": "sts:AssumeRole"
            }
          ]
        }
        EOF
          
          aws iam create-role \
            --role-name ecsTaskExecutionRole-ServerMonitoring \
            --assume-role-policy-document file://trust-policy.json
          
          aws iam attach-role-policy \
            --role-name ecsTaskExecutionRole-ServerMonitoring \
            --policy-arn arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy
          
          echo "Waiting for role to be available..."
          sleep 10
        else
          echo "ecsTaskExecutionRole-ServerMonitoring already exists"
        fi
        
        # Create ECS Task Role
        TASK_ROLE_EXISTS=$(aws iam get-role --role-name ecsTaskRole-ServerMonitoring --query 'Role.RoleName' --output text 2>/dev/null || echo "None")
        
        if [ "$TASK_ROLE_EXISTS" = "None" ]; then
          echo "Creating ecsTaskRole-ServerMonitoring..."
          
          aws iam create-role \
            --role-name ecsTaskRole-ServerMonitoring \
            --assume-role-policy-document file://trust-policy.json
          
          echo "Waiting for role to be available..."
          sleep 10
        else
          echo "ecsTaskRole-ServerMonitoring already exists"
        fi

    - name: Create API task definition
      run: |
        cat > api-task-definition.json <<EOF
        {
          "family": "servermonitoring-api",
          "networkMode": "awsvpc",
          "requiresCompatibilities": ["FARGATE"],
          "cpu": "512",
          "memory": "1024",
          "executionRoleArn": "arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/ecsTaskExecutionRole-ServerMonitoring",
          "taskRoleArn": "arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/ecsTaskRole-ServerMonitoring",
          "containerDefinitions": [
            {
              "name": "${{ env.API_CONTAINER_NAME }}",
              "image": "${{ env.API_IMAGE }}",
              "cpu": 512,
              "memory": 1024,
              "essential": true,
              "portMappings": [
                {
                  "containerPort": 8080,
                  "protocol": "tcp"
                }
              ],
              "environment": [
                {
                  "name": "ASPNETCORE_ENVIRONMENT",
                  "value": "Production"
                },
                {
                  "name": "ASPNETCORE_URLS",
                  "value": "http://+:8080"
                },
                {
                  "name": "UseInMemoryDatabase",
                  "value": "true"
                }
              ],
              "logConfiguration": {
                "logDriver": "awslogs",
                "options": {
                  "awslogs-create-group": "true",
                  "awslogs-group": "/ecs/servermonitoring-api",
                  "awslogs-region": "${{ env.AWS_REGION }}",
                  "awslogs-stream-prefix": "ecs"
                }
              },
              "healthCheck": {
                "command": ["CMD-SHELL", "curl -f http://localhost:8080/health || exit 1"],
                "interval": 30,
                "timeout": 5,
                "retries": 3,
                "startPeriod": 180
              }
            }
          ]
        }
        EOF

    - name: Register API task definition
      id: api-task-def
      run: |
        TASK_DEF_ARN=$(aws ecs register-task-definition --cli-input-json file://api-task-definition.json --query 'taskDefinition.taskDefinitionArn' --output text)
        echo "TASK_DEF_ARN=$TASK_DEF_ARN" >> $GITHUB_OUTPUT

    - name: Deploy to ECS
      run: |
        SERVICE_EXISTS=$(aws ecs describe-services --cluster ${{ env.ECS_CLUSTER }} --services ${{ env.ECS_API_SERVICE }} --query 'services[0].status' --output text 2>/dev/null || echo "MISSING")
        
        if [ "$SERVICE_EXISTS" = "ACTIVE" ] || [ "$SERVICE_EXISTS" = "DRAINING" ]; then
          echo "Service exists with status: $SERVICE_EXISTS, deleting..."
          aws ecs update-service --cluster ${{ env.ECS_CLUSTER }} --service ${{ env.ECS_API_SERVICE }} --desired-count 0 2>/dev/null || true
          aws ecs delete-service --cluster ${{ env.ECS_CLUSTER }} --service ${{ env.ECS_API_SERVICE }} 2>/dev/null || true
          
          echo "Waiting for service to be fully deleted..."
          for i in {1..30}; do
            SERVICE_STATUS=$(aws ecs describe-services --cluster ${{ env.ECS_CLUSTER }} --services ${{ env.ECS_API_SERVICE }} --query 'services[0].status' --output text 2>/dev/null || echo "MISSING")
            echo "Attempt $i: Service status = $SERVICE_STATUS"
            
            if [ "$SERVICE_STATUS" = "INACTIVE" ] || [ "$SERVICE_STATUS" = "MISSING" ]; then
              echo "Service fully deleted!"
              break
            fi
            
            if [ $i -eq 30 ]; then
              echo "Timeout waiting for service deletion!"
              exit 1
            fi
            
            sleep 10
          done
        fi
        
        echo "Creating service..."
        
        SUBNETS=$(aws ec2 describe-subnets --query 'Subnets[*].SubnetId' --output json)
        echo "Subnets: $SUBNETS"
        
        VPC_ID=$(aws ec2 describe-subnets --query 'Subnets[0].VpcId' --output text)
        echo "VPC: $VPC_ID"
        
        if [ "$SUBNETS" = "[]" ] || [ -z "$SUBNETS" ]; then
          echo "ERROR: No subnets available!"
          exit 1
        fi
        
        # Check if security group exists
        EXISTING_SG=$(aws ec2 describe-security-groups \
          --filters "Name=group-name,Values=servermonitoring-api-sg" \
          --query 'SecurityGroups[0].GroupId' \
          --output text 2>/dev/null || echo "None")
        
        EXISTING_SG_VPC=$(aws ec2 describe-security-groups \
          --filters "Name=group-name,Values=servermonitoring-api-sg" \
          --query 'SecurityGroups[0].VpcId' \
          --output text 2>/dev/null || echo "None")
        
        # Delete if in wrong VPC
        if [ "$EXISTING_SG" != "None" ] && [ "$EXISTING_SG_VPC" != "$VPC_ID" ]; then
          echo "Deleting old security group in wrong VPC..."
          aws ec2 delete-security-group --group-id $EXISTING_SG 2>/dev/null || true
          sleep 5
        fi
        
        # Create security group in correct VPC
        SG_ID=$(aws ec2 create-security-group \
          --group-name servermonitoring-api-sg \
          --description "Security group for Server Monitoring API" \
          --vpc-id $VPC_ID \
          --query 'GroupId' \
          --output text 2>/dev/null || \
          aws ec2 describe-security-groups \
            --filters "Name=group-name,Values=servermonitoring-api-sg" "Name=vpc-id,Values=$VPC_ID" \
            --query 'SecurityGroups[0].GroupId' \
            --output text)
        
        echo "SG: $SG_ID"
          
        # Allow inbound traffic
        aws ec2 authorize-security-group-ingress \
            --group-id $SG_ID \
            --protocol tcp \
            --port 8080 \
            --cidr 0.0.0.0/0 2>/dev/null || true
        
        # Allow outbound traffic (needed for ECR image pull)
        aws ec2 authorize-security-group-egress \
            --group-id $SG_ID \
            --protocol -1 \
            --cidr 0.0.0.0/0 2>/dev/null || true
          
        # Create service with JSON format
        aws ecs create-service \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service-name ${{ env.ECS_API_SERVICE }} \
            --task-definition ${{ steps.api-task-def.outputs.TASK_DEF_ARN }} \
            --desired-count 1 \
            --launch-type FARGATE \
            --network-configuration "{
              \"awsvpcConfiguration\": {
                \"subnets\": $SUBNETS,
                \"securityGroups\": [\"$SG_ID\"],
                \"assignPublicIp\": \"ENABLED\"
              }
            }"


    - name: Check service creation
      run: |
        echo "Waiting for service to start..."
        sleep 30
        
        # Check if service was created
        SERVICE_STATUS=$(aws ecs describe-services \
          --cluster ${{ env.ECS_CLUSTER }} \
          --services ${{ env.ECS_API_SERVICE }} \
          --query 'services[0].status' \
          --output text)
        
        echo "Service status: $SERVICE_STATUS"
        
        # Get task details
        TASKS=$(aws ecs list-tasks \
          --cluster ${{ env.ECS_CLUSTER }} \
          --service-name ${{ env.ECS_API_SERVICE }} \
          --query 'taskArns' \
          --output json)
        
        echo "Tasks: $TASKS"
        
        # If no tasks, check service events
        if [ "$TASKS" = "[]" ]; then
          echo "No tasks created! Checking service events:"
          aws ecs describe-services \
            --cluster ${{ env.ECS_CLUSTER }} \
            --services ${{ env.ECS_API_SERVICE }} \
            --query 'services[0].events[0:5]' \
            --output table
          exit 1
        fi
        
        # Get task status
        TASK_ARN=$(echo $TASKS | jq -r '.[0]')
        TASK_STATUS=$(aws ecs describe-tasks \
          --cluster ${{ env.ECS_CLUSTER }} \
          --tasks $TASK_ARN \
          --query 'tasks[0].lastStatus' \
          --output text)
        
        echo "Task status: $TASK_STATUS"
        
        # Check for stopped tasks
        STOPPED_REASON=$(aws ecs describe-tasks \
          --cluster ${{ env.ECS_CLUSTER }} \
          --tasks $TASK_ARN \
          --query 'tasks[0].stoppedReason' \
          --output text 2>/dev/null || echo "None")
        
        if [ "$STOPPED_REASON" != "None" ] && [ "$STOPPED_REASON" != "" ]; then
          echo "Task stopped! Reason: $STOPPED_REASON"
          
          # Get container details
          aws ecs describe-tasks \
            --cluster ${{ env.ECS_CLUSTER }} \
            --tasks $TASK_ARN \
            --query 'tasks[0].containers[0]' \
            --output json
          exit 1
        fi

    - name: Wait for deployment (with timeout)
      run: |
        echo "Waiting for service to stabilize (max 10 minutes)..."
        
        timeout 600 aws ecs wait services-stable \
          --cluster ${{ env.ECS_CLUSTER }} \
          --services ${{ env.ECS_API_SERVICE }} || {
          echo "Deployment timed out or failed!"
          echo "Service events:"
          aws ecs describe-services \
            --cluster ${{ env.ECS_CLUSTER }} \
            --services ${{ env.ECS_API_SERVICE }} \
            --query 'services[0].events[0:10]' \
            --output table
          
          echo "Task failures:"
          aws ecs describe-services \
            --cluster ${{ env.ECS_CLUSTER }} \
            --services ${{ env.ECS_API_SERVICE }} \
            --query 'services[0].events[?contains(message, `failed`)]' \
            --output table
          exit 1
        }

    - name: Get service URL
      run: |
        TASK_ARN=$(aws ecs list-tasks --cluster ${{ env.ECS_CLUSTER }} --service-name ${{ env.ECS_API_SERVICE }} --query 'taskArns[0]' --output text)
        
        if [ "$TASK_ARN" = "None" ] || [ -z "$TASK_ARN" ]; then
          echo "No tasks running!"
          exit 1
        fi
        
        ENI_ID=$(aws ecs describe-tasks --cluster ${{ env.ECS_CLUSTER }} --tasks $TASK_ARN --query 'tasks[0].attachments[0].details[?name==`networkInterfaceId`].value' --output text)
        PUBLIC_IP=$(aws ec2 describe-network-interfaces --network-interface-ids $ENI_ID --query 'NetworkInterfaces[0].Association.PublicIp' --output text)
        
        echo "========================================="
        echo "API: http://$PUBLIC_IP:8080/swagger"
        echo "Health: http://$PUBLIC_IP:8080/health"
        echo "========================================="

